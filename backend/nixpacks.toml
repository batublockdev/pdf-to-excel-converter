[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install fastapi uvicorn[standard] python-multipart pdfplumber pandas openpyxl google-generativeai python-dotenv']

[start]
cmd = 'uvicorn main:app --host 0.0.0.0 --port $PORT'